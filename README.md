
## Calendar (react/redux + node/mongo)

- For running front:

  - cd front/
  - npm i
  - npm run start
  - open in browser localhost:3000
  
- For running server:

  - cd server/
  - npm i
  - npm run start
  
  
